# Hyperframe Studio — environment variables

# SQLite database location (Prisma datasource URL).
# A file:// URL pointing to a local SQLite database file.
DATABASE_URL=file:/home/z/my-project/db/custom.db
